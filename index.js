// code your solution here
function saturdayFun(name = "I", activity = "roller-skate") {
    console.log(`${name} want to ${activity}`);
}

function mondayWork(name = "I", activity = "go to the office") {
    console.log(`${name} want to ${activity}`);
}

function wrapAdjective() {
const wrapAdjective = ("*") ("are someone special");
const wrapAdjective = ("!!")
}
